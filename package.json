{
  "name": "cypress-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "live": "live-server --port=8080",
    "cypress:run:prod": "cypress run --e2e --browser chrome --env master=1",
    "cypress:run:local": "cypress run --e2e --browser chrome",
    "cypress:run:test": "live-server --port=8080 & cypress run --e2e",
    "cypress:open": "cypress open"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.9.0",
     "live-server": "^1.2.1"
  }
}
