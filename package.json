{
  "name": "cypress-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "live": "live-server",
    "cypress:run:prod": "cypress run --e2e --browser chrome --env master=1",
    "cypress:run:test": "live-server && cypress run --e2e --browser chrome",
    "cypress:open": "cypress open"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.9.0"
  }
}
