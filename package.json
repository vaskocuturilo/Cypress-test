{
  "name": "cypress-test",
  "version": "1.0.0",
  "description": "",
  "default": "index.html",
  "scripts": {
    "cypress:run:prod": "cypress run --e2e --browser chrome --env master=1",
    "cypress:run": "cypress run --e2e",
    "cypress:run:tests": "start-server-and-test start http://localhost:1234 cypress:run",
    "start": "parcel src/index.html",
    "build": "parcel build src/index.html --dist-dir ./dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.9.0",
    "parcel": "^2.8.3",
    "start-server-and-test": "^2.0.0"
  }
}
